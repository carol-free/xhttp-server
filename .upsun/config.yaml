applications:
    app:
        type: nodejs:20
        web:
            commands:
                start: node server.js
            locations:
                /:
                    allow: true

routes:
    "https://{default}/":
        type: upstream
        upstream: "app:http"
